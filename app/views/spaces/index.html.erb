

<div class="container">
  <h2>Explore</h2>
  <br>
  <div class="row">
      <% @spaces.each do |space| %>
        <div class="col-sm-6 col-md-4 col-lg-3">
          <%= link_to (render partial: 'shared/index_card', locals: { space: space }), space_path(space) %>
        </div>
    <% end %>
  </div>
</div>
